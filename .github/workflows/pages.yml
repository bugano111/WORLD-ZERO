name: Legacy Pages workflow disabled
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled; deploy.yml publishes WORLD ZERO."
